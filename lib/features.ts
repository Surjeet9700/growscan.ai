export const FEATURES = {
  // Commerce is intentionally hidden for public launch until affiliate access
  // and product inventory are production-ready.
  commerce: false,
  // Full-screen splash should feel native only inside installed app mode.
  standaloneSplashOnly: true,
} as const;
